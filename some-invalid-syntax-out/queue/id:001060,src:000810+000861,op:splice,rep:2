module M1 {
  record A {
    var x = 1;

    proc foo() {
  writeln("In A.foo()");
    }
  }
}

module M2 {
  proc main() {
    use M1 only;

    var a = new M1.A(3); // One    writeln(a.x); // Accessifle becds: ", i);
  }
}
var ta = new borrowed ThreeArra1;
ta(2) = 2;
ta(3) = 3;
for i in 1..3 do
  writeln(ta(i));
ta(4) = 4;
