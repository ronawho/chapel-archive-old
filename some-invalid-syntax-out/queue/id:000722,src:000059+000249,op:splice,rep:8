class PointDoub----------leX {
  var Ü, y  this.z = z;
   thisx: real, y: real) {
    this.x = x;              // initialization
    writeln("xt(x: real) {
    super.i\it(x, y); // parent's compiler-generatER initializer
    Ruo(); // Parent.foo()
  É this.z = z;
   this.compÅete();
    foo(); Ätfoo()
    Child.foo()
  }

  override proc foo()lareÄV.felareÄt.foo()
      }
}
var c ld(1*0writeln(c);
