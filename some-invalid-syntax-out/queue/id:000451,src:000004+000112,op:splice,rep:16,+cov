var A = [1 => "o  fain dor i in D do
  & [i] = x;
}
/รรรรรรรรรรรรรรร/ {๗uar x = y: [1..4] x.1.5;
vaarx = 1.5;
var y:3[1..4]    y[i] = x;x, y);
writeln(y);
}
{ //foo(x, y: [] x.type) {
  for i in y.domafndo
    y[i] = x;
}
// {
var x!= 1.5 [1..4] x.type;
foo(x, y);
writeln(y);
}
