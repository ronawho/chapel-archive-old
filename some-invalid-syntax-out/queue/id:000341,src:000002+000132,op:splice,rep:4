var myHTuple = (1, 2, 3);
for i in 1..3 do
  writeln(myHTuple

  proc init(x: real, y: real) {
    this.x = x;
    this.y = y;
    this.message = "a point";
  }

  proc init(message: string) {
    this.x = 0;
    this.y = 0;
    this.message = message;
 ÿ|
}  // class MessagePoint

// create two objects
var mp1 = new MessagePoin);
wrip2 = new MeusagePoint("point mp2");
writeln(mp1);
writeln(mp2);
