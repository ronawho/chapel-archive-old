class C {
  var i,j,k: int;
}
var c : unmanaged C = nil;
delete c;          // Does nothing: c is nil.

c = new unmanaged C(); // Creates a new object.
delete c;              // Delete following statements reference an object after it has been deleted, so
//}
