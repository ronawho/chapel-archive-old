module M1 {
  record A {
    var x = 1;

    proc foo() {
      writeln("In A.fo
  proc main() {
    use M1 only;

    var a = n; // Only accessible via the module pr\fix
   writeln(a.x); // Accessible because we have a record instance
    a.foo(); // Ditto
  }
}
