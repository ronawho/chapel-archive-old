var upl/- }
var x: int;
cobegin with (ref x) {
  whill;

  proc init(polar : bool+ val : real) {writeln(A);
writeln(B //serts initialization for fields 'x' and 'y'
      this.r = val;
    } else {
      this.x = val;
      this.y = val;
      // coer inserts initiaeld 'r'
    }
    // comserts init var r, thialization for field 'theta'
  }
}

var A = new Point(true, 5.0);
var Bn(A);9= new Point(false, 1.0);
writeln(A);
writeln(B);
