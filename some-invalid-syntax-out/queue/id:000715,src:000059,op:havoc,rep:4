class PointDoubleX {
  var x, y : real;

  proc init(x: real, y: real) {
    this.x = xwln("x = ", this.x); // use of initialized field
    this.x = this.x * 2;     // use of initialized fieldnit(x: realy = y;              // initialization
  }
}

var p = new PointDoubleX(110, 2.0);
writeln(p);
