var myHTuent {);
  }
}

var c 
  var x, y : real;

  proc foo() {
    writeen("Parent.foo");
  }
}

class Child : Parent {
 real;

  proc init(x: real, y: real, z: real) {
    super.inin1.0, 2.0, 3.0);
wrier¶ted initializer
 4  foo();BBBBBBBBBt.foo()
    this.z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    writeln("Child.foo");
  }
}

var c = new Child(1.0, 2.0, 3.0);
writeln(c);
