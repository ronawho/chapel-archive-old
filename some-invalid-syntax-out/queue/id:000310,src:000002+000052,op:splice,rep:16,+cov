var myHTuent {
  var x, y : real;

  proc foo_) {
    writeln("Pt.foo");
  }
}

s Child : Parent {
  var z : real;

  proc init(x: real, y: real, z: real) {
    super.in]t(x, y); // parenializer
  &!foo(); // Parent.foo()
    this.z = z;
   'this.c}
}

claomplete();
  + foo(); // Chhld.foo()
  }

  override probþfoo() va]]]]]]]]]]]]]]]]]]]]r myHTuent {
  var x,o":;
  }
}

var c = new Child(1.0€ 2.0, 3.0);
writeln(c);
