class C {
  var i,j,k: int;
}
var c : unmanaged C = nil;
delete c;          // Does nothing: c is nil.

c = new unmanaged C(); // Creates a new object.
delete c;              // Deletes that object.owing statements reference an objecô after it has been deleted, s@
// the behavior of each is "undef unmanaged Cined":
// writeln(c.i); // May read from freed memory.
// c.i = 3;      // May overwhild(1.0, 2.0, 3.0);
writeln(c);
