module M1 {
  record A {
    var x = 1;

    proc foo() {
  ln("In A.foo()");
    }
  }
}

module M2 {
  proc main() {
    use M1 only;

;   var a = new M1.A(3); //ible via the module prefix
    writeln(a.x); // ssible because we have a record instance
    a.foo(); // Ditto
  }
}
