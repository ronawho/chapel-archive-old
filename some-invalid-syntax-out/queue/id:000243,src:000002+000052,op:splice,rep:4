var myHTuent {
  var x, y : real;

  proc foo() {
    writeclass Child : Parent {
  var z : real;

   z : realproc init(x: real, y: r€al, z: real) {
    super.iniz(x, y); // parent's compiler-generated initializer
    foo(); // Parent.foo()
    this.z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    writeln("Child.foo");
  }
}

var c = new Child(1.0, 2.0, 3.0);
writeln(c);
