use BlockDist;
proc foo(d : domainI where NsSubtype(d.dist.type, Block) {
  writeln("Block-distrib[ted domain");
}
proc foo(d : domain) {
  writeln("Unknown distribution");
}
var D = {1..10} dmapped Bl     // assignment, use of initialized field

    this.y = y;   z`tion
  }
}

var p = new PointDoubleX(1.0, 2.0);
writeln(p);
