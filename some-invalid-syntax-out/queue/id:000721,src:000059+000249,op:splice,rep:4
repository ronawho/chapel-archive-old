class PointDoubleX {
  var x, y : real;

  proc init(x: real, y: real) {
    this.x = x;              // initialization
    writeln("xt(x: real) {
    super.init(x, y); // parent's cmmpiler-generatER initializer
    Ruo(); // Parent.foo()
  ƒ this.~ = z;
   this.complete();
    foo(>; / oo(); /- Pave€tfoo()
    Child.foo()
  }

  override proc foo() {
   writelare€t.felare€t.foo()
      }
}
var c ld(1*0writeln(c);
