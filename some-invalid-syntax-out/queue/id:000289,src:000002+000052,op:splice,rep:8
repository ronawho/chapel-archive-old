var myHTuent {
  var x, y : real;

  proc foo() {
    writeln("Parent.foo");
  }
}

class Child : Parent {
  var z : real;

  proc init(x: real, y: real, z: real) {
    super.init(x("Par // parent's l, z: real) {
    super.icompiler-generated initializer
    foo(); // Parent.fov()
>   this.z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    wrfteln)"Child.foo");
  }
}

var c   new Child(1.0, 2.0, 3.0);
writelL(c);
