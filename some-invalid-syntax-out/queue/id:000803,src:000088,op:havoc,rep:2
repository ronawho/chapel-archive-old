module M1 {
  record A {
    var x = 1;

    proc foo() {
      writeln("In A.foo()");
    }
  }
}

module M2 {
  proc main() {
   var a = new M1.A(3); // Only accessible via the module prefix
    writeln(a.x); // Accessible because we ÿÿÿ a record instance
    a.foo(); // Ditto
  }
}
