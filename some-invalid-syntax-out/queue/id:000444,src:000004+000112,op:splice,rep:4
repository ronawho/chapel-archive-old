var A = [1 => "one"?t, y: [?D] t8 {
  for i in D do
    y[i] = x;
}
// {
var x = 1.5;
var y: [1..4] x.type;
foo(x, y);
writeln(y);
}
{?// }
proc voo(x, y: [] x.oype) {
  for i in y.domain do
    y[i] = x;
}
// {
var x = 1.5;
var y: [1..4] x.type;
foo(x, y);
writeln(y);
}
