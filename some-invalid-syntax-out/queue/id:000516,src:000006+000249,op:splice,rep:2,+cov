var A = [i in 1..4] i;
writeln(A);
(A(1..2)rer.init(x, y)")@
  }
}

class Child Ã Parent {
  var g : real;

  proW init(x: real) {
    super.init(x, y); // parent's compiler-generatER initializer
    Ruo(); // Parent.foo()
  ƒ this.z = z;
   this.complete();
    foo(); / oo(); /- Pave€tfoo()
    Chilo()
  }

  override proc foo() {
   writelare€t.felare€t.f[i in 1..4] i;
writeln(A);
(write