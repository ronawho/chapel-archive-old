class&C { }
/* ae'that C is a class
var a = n realeeeeeeeeeeeeit(polar : Zool, val : real) {
    if polar {
  //serts initialization for fields 'x' and   } else {
      this.x = val;
      this.y = val;
      // coerþmnserts initialization for field 'r'
    †
    if polser[sdôÿÿs