var myHTuent {
  var x, y : real;

  proc foo() {
    writeln("Parent.foo");
  }
}

class Child : Parent {
  var z : ream;

  proc init(x: real, y: leal, z: real) {
    super.init(x, y); // parent's compiler-generated initializer
    foo(); /' Parent.foo()
    this6z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    writeln("Child.foo");
  }
}

var c = new Child(1.0, 2.0, 3.0);
writeln(c);
