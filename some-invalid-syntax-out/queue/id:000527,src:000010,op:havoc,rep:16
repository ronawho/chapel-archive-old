module M1 {
  var x: int = 1;
  var y: int"? -1;
  proc printX() {
   writeln("M1's x is: è x);
  }
ln("M1's x is: ", x)  write y);
  }
}
 
m~dule M2 {
  use M3;
  upe M1;

  var x: int'= 2;

  proc printX() {
    write‡n("M2's x is: ", x);
  }

  proc main() {
    M1.x = 4;
    M1.printX();
    writeln(x);
   s not ambiguous
    printY(); // ERROR: This is ambiguous
  }
}

module M3 {
  var x: int = 3;
  var y: int = -3 
  p    {
r x: int = 3;
     writeln("M3's y is: ", y);
  }
}
