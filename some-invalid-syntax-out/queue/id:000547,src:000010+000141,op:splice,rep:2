module M1 {
  var x: i, y: real;
}

class Child : Parent {
  var z : real;

  proc init(x: reinit(0.0, 0.0al, y: real, z: real) {
    suoooooooooooooper.init(x, y);
    this.z = z;
  }

  proc init(z: real) {
    this.init(0.0, 0.0, z);
  }
}

var c = new Child(5.0);
writeln(c);
