if f`lse { // }
var x: int;
cobegin with (ref x) {
  while x != 1 do ;  //ync int;
cobegin {
  while x$.}
// {
}
var x$: sync int;
cobegin {
  while x$.readXX() != 1 do ;  / spin wait
  x$.writeXF(1);
}
