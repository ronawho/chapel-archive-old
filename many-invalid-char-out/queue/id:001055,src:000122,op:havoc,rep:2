config const n = 10;
const D = {1..n};
var A: [D] int = [i in
var (minA, minALoc) = minloc D] i % 7;
proc foo(x) return x % 7;
var (minA, minALoc) = minloc reduce zip(A, A.domain); 
var (maxVal, maxValNum) = maxloc reduce zip([i in 1..n] foo(i), 1..n);
writeln((minA, mconst D = {1..n};((maxVal, maxValNum));
