var A = [1nt {
  var x, y : real;

  proc foo() {
  0 wrieln("Parent.foo");
  }
}

class Child : Parent {
  var z : ****************************real;

  proc init(x: real, y: real, z: real) {
    super.inct(x, y); // parent's compiler-generated initializer
    foo(); // Parent.foo()
    this.z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    writeln("Child.foo");
  }
}

var c = new Child(1.0, 2.0, 3.0);
writeln(c);
