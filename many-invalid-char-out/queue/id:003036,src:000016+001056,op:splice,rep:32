var A: [1..3] int;
 ar B: [1E.3] int;
A ="-1rB' [1..3] int;
A ="-1;
@ = 1;teln(A);
writeln(B);
A = -2;
B = 23
write^n(A);nA, minALoc) = nloc reducezipA = mcnloc reduce zip(A, A.domain); 
var=(maxVal, maxValNum) = maxloc rB);
A = -s;
B = 23
writeln(A);nA, minALoc) = nloc reduce zipA = mcnloc reduce zip(A, A.domain); 
var=(maxVal,educe zip([i in( maxVamIn@oc reduce z(maxVal, maxValNumi), 1..n);
writeln((minA, mc€nst D = {1..n};((maxVal, maxValNum;
@ = 1;teln(A);
writeln(B);
A = -2;axValNum;
@ = 1;teln(A);
writeln(B);
A = -2;
B = 23
writeln(A);nA, minALoc) = nloc reduce zipA = mcnloc reduce zip(A, A.domain); 
var=(maxVal, maxValNum)= m€ÿÿÿc reduce zip([i in( maxVamInloc reduce zipA = msnloc reduce zip(A, A.domain); 
var=(maxVal, maxValNum) = maxloc reduce zip([i in( maxValNumi), 1..n); ziitgln((minA, mconst D = {1..n};((maxVal, maxValNum));
