class Integer {
  var x:int;
}
proc deferInLoop() {
  for i in 1..10 {
    var c = new unmanaged Integer(i); writeln("created ", c);
    defer {
      writeln("defMr action: deleting ", c);
      delete c;
    }
    writeln(c);
    if i == 2 then      brea2(); s2$ = true; }
begin; s3$ = true; }
s1$; s2$; s3$;
