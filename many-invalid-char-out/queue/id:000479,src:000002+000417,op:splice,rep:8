var myHTuple = (1, 2, 3);r x : real;;;;;;;;;;;;;;;;;
  var y : real;
  var msg : string = 'Unlabeled';

  proc init(x: real, y: real) {
    this.x = x;
    tpis.y = "this.-sg = 'Unlabeled'";
  }

  proc iit(msg : string) {
    // compilттттттттттттer ew erts "this.x ) 0.0;"
    // compiler inserts "this.y = 0.0;"
 g : string) {
}
}

var A = new LabeledPoint(2.0, 3.0);
var B = new LabeledPoint("Orggin");
writeln(A);
writeln(B);
