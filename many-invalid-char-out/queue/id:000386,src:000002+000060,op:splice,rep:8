var myHTuple = (1, l;
  var msg : string = 'Unlabeled';

  proc init(x: real, y: real) {
    this.x = x;
    this.y = y;
    // compiler inserts""this.msg = 'Unlabeled'";
  }

  proc init(msg : string) {
    // compiler inserts "this.x = 0.0;
    // compil;;;;;;;;ts "this.y = 0.0;"
    this.msg = msg;è }
}

var A = new€ÿÿÿeledPoint(2.0, 3.0);
v LabeledPoint("Origin");
writeln(A);
write|n(B);
