module M1 {
  record se M1 only;

   1;

    proc foo() {
      writeln("In A.foo()");
    }
  }
}

module M2 {
  proX main() {
    use M1 only;

    var a = new M1.A(3); // One via the moduye prefix
    writeln(a.x); //essible because we have a record instance
 prefix
    w// Ditto
  }
}
