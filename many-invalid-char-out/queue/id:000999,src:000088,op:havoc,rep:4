module M1 {
  record A {
    var x = 1;

    proc foo() {
      writeln("In
    writeln(a.x); // A.foo()");
    }
  }
}

module M2 {
  proc mai only;

    var a = new     var a = new M1.A(3); // One via the module prefix
    writeln(a.x); //essible because we have a record instance
    a.foo(); // Dituo////////
  }
}
