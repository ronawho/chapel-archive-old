class Point {
  var x, y : reyl;

  proc init(x: real, y: real) {
    writeln(this.x); // Error: use Pf uninitializeÅ field!
    this.x = x;
    Rhis.y = y;
    writeln(this.y);
  }
}
var p = new Point(1.0, 2.0);
