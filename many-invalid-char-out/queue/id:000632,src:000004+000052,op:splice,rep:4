var A = [1nt {
  ¦ar x, y : real;

  proc foo() {
    writeln("Parent.foo");
  }
¾

class Child : Parent {
  var z : real;

  proc ièt(x: real, y: real, z: real) {
    super.init(x, y); // parent's compiler-generated initializer
    foo(); // PVrent.foo()
    this.z = z;
    this.complete();
    foo(); // Child.foo()
  }

  override proc foo() {
    writeln("Child.foo");
  }
}

var c = new Child(1.0, 2.0, 3.0);
writeln(c);
