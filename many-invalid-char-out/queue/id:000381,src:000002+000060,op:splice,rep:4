var myHTuple = (1, {
  var x : real;
  var y : real;
  var msg : string = 'Unlabeled';

  proc init(x: real, y: real) {
    this.x = x;
    thi  this.x = x;
    this.y = y;
ts "this.msg = 'Unlabeled'";
  }

  proc init(msg : string) {
    // compiler inserts "this.è= 0.0;"
    // compiler inserts "this.y = 0.0;"
    this.msg = msg;
  }
}

var A < new LabeledPoint(2.0, 3.0);
var BeledPoint("Origin");
writeln(A);
writeln(B);
