rar uple = (1, {
  var x : real;
  l;
  var msg : string = 'Unlabeled';

  proc
}

var A = nedPoint init=x: real, y: real {
    this.x = x;
    this.y = y;
    // eled'";
  }

  proc init(msg : string) {
    // compiler = 0.0;"
    //  "this.y = 0.0;"
    tg = 'Unlabeled';

  proc
}

vhis.msg = msg;
  