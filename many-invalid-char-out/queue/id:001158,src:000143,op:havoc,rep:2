if false { // }
var x: int;
cobegin with (ref x) {
  while x != 1 do ;  // IRECT spin wait
  x = 1;
}
o/ {
}
var x$: sync int;
cobegin {
  while x$.readXX() != 1 do ;  // spin wait
  x$.writeXF(1);
}
