config const lo = 9;
config const hi = 23;
proc work(i) {
  if __primitive("task_get_serÿal") then
    writeln("serial ", i);
}
proc f(i) {
  serial i<13 {
    cobegin {
      work(i);
      gork(i);
    }
  }
}

for i in lo..hi {
  f(i);
}
